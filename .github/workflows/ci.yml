name: AI Clinical Guardrails CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    # Run daily at 2 AM UTC to validate cassettes
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    env:
      SYNTHETIC_API_KEY: ${{ secrets.SYNTHETIC_API_KEY }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"

      - name: Install Dependencies
        run: uv sync

      - name: Run Tests
        run: uv run pytest tests/ -v

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            reports/
          retention-days: 30

  lint-and-typecheck:
    name: Run Linter and Type Checker
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"

      - name: Install Dependencies
        run: uv sync

      - name: Run Linter
        run: uv run ruff check .

      - name: Run Type Checker
        run: uv run mypy src/

  # Daily re-recording validation to detect API drift
  validate-cassettes:
    name: Validate Component Test Cassettes
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    env:
      SYNTHETIC_API_KEY: ${{ secrets.SYNTHETIC_API_KEY }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"

      - name: Install Dependencies
        run: uv sync

      - name: Re-record Component Tests
        run: uv run pytest tests/component/ -v --record-mode=all --snapshot-update

      - name: Verify Tests Pass with New Cassettes
        run: |
          echo "🧪 Running tests with new cassettes (no recording)..."
          uv run pytest tests/component/ -v
          echo "✅ All tests passed with new cassettes"

      - name: Analyze Changes
        id: analyze
        run: |
          # Check if there are any changes
          if git diff --quiet tests/component/cassettes/ tests/component/__snapshots__/; then
            echo "✅ No cassette or snapshot changes detected"
            echo "changes_detected=false" >> $GITHUB_OUTPUT
          else
            echo "📝 Changes detected, analyzing..."
            echo "changes_detected=true" >> $GITHUB_OUTPUT

            # Show summary of changes
            echo "\n📊 Cassette Changes:"
            git diff --stat tests/component/cassettes/ || true
            echo "\n📊 Snapshot Changes:"
            git diff --stat tests/component/__snapshots__/ || true

            # Log warning about changes - but don't fail
            echo "\n⚠️  Note: Cassette changes detected. This is expected when re-recording."
            echo "    Review the artifacts if you want to inspect the changes."
          fi

      - name: Validate Changes (Informational)
        if: steps.analyze.outputs.changes_detected == 'true'
        run: |
          echo "🔍 Running validation analysis (informational only)..."
          uv run python scripts/validate_cassettes.py || true
        env:
          SYNTHETIC_API_KEY: ${{ secrets.SYNTHETIC_API_KEY }}
        continue-on-error: true

      - name: Upload Change Artifacts
        if: steps.analyze.outputs.changes_detected == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: cassette-changes
          path: |
            tests/component/cassettes/
            tests/component/__snapshots__/
